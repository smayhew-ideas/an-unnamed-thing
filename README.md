
  # coded version

  This is a code bundle for coded version. The original project is available at https://www.figma.com/design/SMdOLFZ5Uj9qEI4B02mFkO/coded-version.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  